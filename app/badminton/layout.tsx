import { PlateShell } from '@/components/layout/PlateShell'

export default function BadmintonPlateLayout({ children }: { children: React.ReactNode }) {
  return <PlateShell>{children}</PlateShell>
}
